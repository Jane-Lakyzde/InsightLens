# pnpm 配置文件

# 使用扁平化的 node_modules 结构，提高兼容性
node-linker=hoisted

# 自动安装缺失的 peer 依赖
auto-install-peers=true

# 严格检查 peer 依赖
strict-peer-dependencies=false

# 启用快速安装
prefer-offline=true

# 允许在任何地方运行脚本
enable-pre-post-scripts=true

# 禁用包更新检查以提高安装速度
update-check-interval=168
